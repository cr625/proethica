"""Entity-commit and TTL-serialization services.

Topical grouping (services modularization, Phase 5): the case-commit services
that serialize extracted entities to OntServe TTL -- the canonical versioned
writer (ontserve_commit), the step4-UI auto-commit path (auto_commit), and the
temporal-pass committer. Package marker only; import concrete classes from their
submodules.
"""
