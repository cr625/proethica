"""NSPE code-provision handling.

Topical grouping (services modularization, Phase 5) of the loose provision
sub-pipeline -- reference parsing, universal detection, grouping, group
validation, and code-provision linking. These modules are always used together
by the Step-4 synthesis paths. Package marker only; import the concrete classes
from their submodules (e.g. ``from app.services.provision.code_provision_linker
import CodeProvisionLinker``).
"""
