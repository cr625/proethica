"""MCP transport + OntServe client/config/data-access services.

Topical grouping (services modularization, Phase 5) of the loose MCP/OntServe
infrastructure: the JSON-RPC/SSE transport, the MCP clients, on-demand entity
enrichment, OntServe config (paths/db/urls), the data fetcher, and the annotation
service. Package marker only; import concrete classes/functions from their
submodules.
"""
