"""Embedding and vector-similarity services.

Topical grouping (services modularization, Phase 5): the sentence/section
embedding services and the shared pure-Python cosine helper. Package marker only;
import concrete classes/functions from their submodules.
"""
